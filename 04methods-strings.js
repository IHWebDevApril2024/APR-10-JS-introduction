const userName = "Oriol";

// SLICE METHOD

console.log(userName.slice(1,5)) // Is not returning the character of the second index

// LOCALE COMPARE

const user1 = "Marcel";
const user2 = "Marcelo";


console.log(user1.localeCompare(user2))  // -1 if user1 goes first, 0 if they are the same and 1 if user2 goes first


const phrase = "Javascript is amazing, nothing is impossible now, I feel superpowered"

console.log(phrase.includes("!")); // false because phrase doesn't include the exclamation

